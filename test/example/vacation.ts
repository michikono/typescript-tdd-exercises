/// <reference path="../../references.ts" />
import assert = require("assert");
require('sinomocha')();

describe('Example Module', function () {
    describe('destination()', function () {
        it('should do something', function () {
        });
    });
    describe('travelDuration()', function () {
        it('should do something', function () {
        });
    });
});
